#include <stdio.h>

int main() {
    // declare variable w/ const, declare type w/ int
    const int TRUE = 1;
    const int FALSE = 0;

    // %d is decimal placeholder, takes argument after comma
    printf("TRUE is represented by %d\n", TRUE);
    printf("FALSE is represented by %d\n", FALSE);
    return 0;
}